# RealTimeApp 設計メモ

個人開発用の設計メモ。要件が固まり次第、随時更新する。

## 1. 概要

複数人が同時に、人物情報を記載した「パネル」を「ライン(横のレーン)」上でドラッグ&ドロップ操作できる共同編集アプリ。単一の共有スペースのみを扱い、複数ボードを切り替えるような「ボード」の概念は持たない。

- パネル: 人の情報を記載したカード。1つのラインに所属する。ユーザーが任意のタイミングで追加・削除できる。
- ライン: パネルを横方向に並べて管理するレーン。画面内には複数のラインが縦方向に積み重なって表示される(各ラインそのものが横方向に伸びる帯であり、ライン同士は上下に並ぶ)。
- 操作: パネルは同一ライン内での並び替え、および異なるラインへの移動(D&D)が可能。加えて、各ラインへのパネルの追加、および個々のパネルの削除が可能。
- 編集中/操作中/確定: パネルのドラッグを開始すると、そのパネルは「編集中」状態になり他ユーザーにも表示される。ドロップした時点では即座に確定せず、ドロップされた側のライン(移動先)とドラッグ元だったライン(移動元)の両方が「操作中」モードになる。ユーザーが確定ボタンを押した時点で初めて操作が確定し、操作履歴に記録される(ドラッグ中・操作中に何度動かしても、確定するまでは履歴を汚さない)。操作中モードは確定前であればキャンセル可能で、キャンセルするとドラッグ開始前の状態に戻る。
- ページング: ライン一覧は10ラインごとにページングして表示する。パネルに対する操作(追加・削除・D&D)は表示中のページ単位で行う。
- 履歴: 「誰が・いつ・どのパネルに対して・どんな操作(追加/削除/移動)を行ったか」を記録し、後から追跡できるようにする。

用途イメージ: 座席表、シフト/アサインボード、チーム編成ボードなど、「人をレーン間で動かす」タイプの管理ツール。

## 2. ゴール / 非ゴール

**ゴール**
- 2〜10人程度が同時に同じ画面を開き、リアルタイムにパネルを動かせる
- 同時操作時の競合を、場当たり的でなくCRDTベースの正式なアルゴリズムで解決する
- ログイン機能を持つ
- 誰がいつどのパネルにどんな操作(追加/削除/移動)を行ったかを操作履歴として記録する(確定した操作のみ)
- まずMVPを素早く動かし、そこから拡張する

**非ゴール(MVPでは扱わない)**
- 大規模同時接続(数百人規模)への最適化
- 権限管理(閲覧専用ユーザーなど)の細分化
- オフライン編集・オフライン同期
- 履歴からのUndo機能、コメント機能
- 複数の共有スペース(ボード)の作成・切り替え

## 3. 想定ユースケース

1. ユーザーがログインして画面を開く
2. 縦に並んだライン一覧が10ラインごとにページングされて表示され、各ライン上に横方向に並んだパネル(人物カード)が表示される。パネルへの操作は表示中のページ単位で行う
3. 複数ユーザーが同時に閲覧している状態で、あるユーザーがパネルのドラッグを開始すると、そのパネルは「編集中」状態になり他ユーザーの画面にも(誰が操作中か含めて)表示される
   - 同じライン内で順番を入れ替える
   - 別のラインへ移動する
4. パネルをドロップすると、移動元・移動先の両ラインが「操作中」モードになる(この時点ではまだ確定・記録されない)。ユーザーが確定ボタンを押すと、その時点で初めて「誰が・いつ・どのパネルを・どう操作したか」が操作履歴として記録される。確定前であれば操作中モードはキャンセル可能で、キャンセルするとドラッグ前の状態に戻る
5. ユーザーはラインに新しいパネルを追加したり、不要になったパネルを削除したりできる。これらの操作も確定時点で履歴に記録される
6. 他の全ユーザーの画面にほぼリアルタイムで変更が反映される
7. 2人が同時に同じパネルを動かしても、データが破損せず収束する

## 4. 技術スタック(提案)

| 領域                   | 選定                                                                     | 理由                                                                                                                                                                                             |
| ---------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| フロントエンド         | React + TypeScript + Vite                                                | 実績豊富、エコシステムが厚い。SPA構成にすることで、Next.jsのサーバーレス実行モデル(Vercel前提)と、常駐が必要なHocuspocus(後述)との相性の悪さを避ける                                             |
| D&D                    | dnd-kit                                                                  | React向けで軽量、アクセシビリティ対応、Yjsとの併用実績あり                                                                                                                                       |
| リアルタイム同期       | Yjs (CRDT)                                                               | 配列/マップの同時編集競合を自動解決する業界標準CRDT。パネルの並び替え・ライン間移動と相性が良い                                                                                                  |
| 同期サーバー           | Hocuspocus (Yjsサーバーフレームワーク) on Node.js                        | 認証フック・永続化・ルーム管理が組み込みで、MVPを素早く構築できる                                                                                                                                |
| Presence(誰が操作中か) | Yjs Awareness Protocol                                                   | 「どのパネルが編集中か」をドラッグ中のみリアルタイムに共有するために使用(MVPで採用)。永続化はせず、確定時にHistoryへ記録する                                                                     |
| DB                     | PostgreSQL                                                               | ユーザーのメタデータ管理、操作履歴(History)の永続化、Yjsドキュメントのスナップショット永続化先としても利用                                                                                       |
| 認証                   | 自前JWT + bcrypt                                                         | 小規模ならメール+パスワードのシンプルな実装で十分。WebSocket接続時にHocuspocusの認証フックへ渡せる自己完結型トークンが必要なため、セッションCookie前提のAuth.js(NextAuth)は採用せずJWTに統一する |
| ホスティング(案)       | フロント: Vercel / バックエンド: Fly.io or Render / DB: Neon or Supabase | 個人開発でランニングコストを抑えやすい構成                                                                                                                                                       |

### なぜYjsか
- パネルの並び替え・ライン間移動は「配列要素の移動」であり、Yjsの `Y.Array` がまさにこのユースケース向けに設計されている
- Operational Transformationを自前実装するよりはるかに低コストで「ちゃんとした」競合解決が手に入る
- 2〜10人規模であれば性能面のオーバーヘッドは問題にならない

**注意: 要素移動の実装方法**
`Y.Array` の要素移動を素朴に「削除→挿入」の2操作で実装すると、2人が同時に同じ要素を別々の位置へ移動した際に要素が重複して残る既知の問題(duplication anomaly)がある。これはゴール7「2人が同時に同じパネルを動かしてもデータが破損せず収束する」に直結するため、実装時は次のいずれかを採用すること。
- Yjs 13.6以降で追加されたネイティブの `Y.Array.prototype.move` (内部的にYATA/Moveアルゴリズムで重複を防ぐ)を使う
- または fractional index(順序を表す小数値をパネル側に持たせる)方式で、配列要素自体は動かさず並び順フィールドだけを更新する

## 5. データモデル(初期案)

**PostgreSQL(永続ストア・メタデータ)**
```
User
  id, name, email, passwordHash, avatarUrl, createdAt

DocSnapshot
  id, ydocState (bytea), updatedAt   -- Yjsドキュメント(単一の共有スペース)の永続化スナップショット

History
  id, userId, panelId, panelName (確定時点の表示名スナップショット), action (add / remove / move),
  fromLineId, toLineId, fromIndex, toIndex, createdAt
  -- ドラッグ中の「編集中」状態、ドロップ後の「操作中」状態は記録せず、確定ボタン押下時に1件だけ記録する
  -- panelNameは操作確定時点の表示名を複製して保持する。パネル削除後はYjs側の実データが
     失われるため、これがないと削除済みパネルの履歴がpanelIdだけになり判読できなくなる
```

- 「編集中」状態はPostgresには保存しない一時的な状態で、Yjs Awareness(メモリ上)のみで管理する
- 確定(ドロップ)した操作だけがHistoryに1件のレコードとして記録される。ドラッグ中に何度移動しても、確定するまではレコードが増えない

## 6. アーキテクチャ概要

```
[Browser A] ── WebSocket ──┐
[Browser B] ── WebSocket ──┼── Hocuspocus Server ── PostgreSQL (snapshot / History永続化)
[Browser C] ── WebSocket ──┘         │
                                REST API(認証・履歴記録) ── PostgreSQL (User / History)
```

- クライアントはYjsのWebSocketプロバイダ経由でHocuspocusサーバーに接続し、単一の共有スペース(ルーム)に参加する
- Hocuspocusの認証フックでログインセッション(JWT等)を検証し、未認証ユーザーは接続拒否
- Y.Docが更新されるたび(=操作が確定するたび)にデバウンスしてバイナリスナップショットをPostgreSQLへ保存し、サーバー再起動時にも状態を復元できるようにする(確定操作自体の頻度は低いため、時間ベースの定期保存よりも変更検知ベースの方がデータロスの窓を小さくでき、無駄な書き込みも抑えられる)
- ドラッグ中(編集中)の位置、およびドロップ後・確定前(操作中)の位置はYjs Awarenessのみで共有し、Y.Doc(永続構造)は変更しない。確定ボタンが押された時点で初めてY.Docを更新し、同時にクライアントがREST API経由でHistoryレコードを1件追加する。これにより、ドラッグ中・操作中の細かい移動が履歴として大量に記録されることを防ぐ。操作中モードは確定前ならキャンセル可能で、キャンセル時はY.Docを変更せずAwarenessの状態のみ元に戻す

### REST APIの役割
Hocuspocusはパネル/ラインのリアルタイム同期のみを担当し、ログイン等のトークン発行や操作履歴の記録はできない。そのため以下を扱う最小限のREST APIが別途必要。

| エンドポイント  | 用途                                                                  |
| --------------- | --------------------------------------------------------------------- |
| `POST /signup`  | ユーザー登録(パスワードハッシュ化してDB保存)                          |
| `POST /login`   | 認証してJWT等のトークンを発行                                         |
| `POST /history` | パネル操作(追加/削除/移動)を確定した際に履歴を1件記録                 |
| `GET /history`  | 記録された操作履歴の一覧取得(MVPでは記録のみで、閲覧UIはロードマップ) |

発行したトークンをフロントエンドがHocuspocusへのWebSocket接続時に渡し、Hocuspocus側の認証フックで検証する。

このREST APIとHocuspocusは別サーバーである必要はなく、同一Node.jsプロセス内に同居させられる。

## 7. MVPスコープ

1. メール+パスワードでのログイン/サインアップ
2. 単一の共有スペースでライン・パネルを表示(ボードの切り替えは対象外)
3. パネルのD&D(ライン内並び替え・ライン間移動)がリアルタイムに同期される
4. ドラッグ中は「編集中」状態、ドロップ後は移動元・移動先ラインが「操作中」状態としてAwarenessで他ユーザーに共有し、確定ボタンが押された操作のみを操作履歴として記録する(誰が・いつ・どのパネルに・何をしたか)。操作中モードは確定前ならキャンセル可能
5. パネルの追加・削除(確定操作として履歴に記録)
6. 同時編集時にYjsにより競合が自動解決される
7. リロード後も状態が保持される(DB永続化)
8. ライン一覧は10ラインごとにページングし、パネルへの操作は表示中のページ単位で行う

## 8. MVP後のロードマップ(案)

- 操作履歴の一覧表示UI(誰が・いつ・何をしたかを閲覧できる画面)
- 履歴からのUndo機能
- パネルの詳細編集(役職・ステータス等のフィールド拡充)
- 権限管理(閲覧専用メンバーなど)
- モバイル対応
- 複数の共有スペース(ボード)対応が将来必要になった場合の再検討

## 9. 実装時の注意(AI利用時のトークン節約)

AIエージェントに実装を依頼する際、無駄なトークン消費を避けるための方針。

- **コマンド実行はユーザーが行う**。npm install・テスト実行・サーバー起動・DBマイグレーション等はAIに実行させず、必要なコマンドを提示させるだけにとどめる
- **修正は差分ベースでやり取りする**。ファイル全文の貼り付け・出力を求めず、変更箇所(行番号・diff)だけを確認する
- **ビルドログ・エラー出力・テスト結果は要約させる**。生のスタックトレース全文を貼るのではなく、原因と該当箇所だけを報告させる
- **型定義・スキーマは一箇所(共有パッケージ等)にまとめて再利用する**。同じ型やDTOを画面ごとに再生成させない
- **コメント・ドキュメントは最小限にする**。自明な説明コメントやdocstringを生成させない(このリポジトリの既定方針)
- **仕様上の決定事項は都度このdesign.mdに追記する**。同じ設計判断を会話のたびに説明し直す必要がないようにし、AIには「design.mdを参照」で済ませる
- **テストは不要**。テストは不要

## 10. 実装済み: 具体的な決定事項

MVP実装(`packages/shared`, `server`, `client`)にあたり確定した詳細。以後はこの節を参照すればよい。

**注意**: 「操作中モード・確定ボタン」(11.1節)は実装済み。Awareness状態の形は下記の通り更新されている。

- **要素移動の実装方式**: 4節で挙げた2案のうちfractional index方式を採用。`Y.Array`は使わず、各パネルを`panels`という`Y.Map`直下の`Y.Map`(フィールド: `id`, `name`, `lineId`, `order`, `createdAt`)として持ち、移動は`lineId`と`order`(隣接パネルのorderの中間値)の更新のみで表現する。配列の削除/挿入をしないため重複異常が原理的に発生しない
- **ライン**: サーバー起動時(`onLoadDocument`)、`lines`という`Y.Map`(プレーンオブジェクト`{id,name,order}`)が空であれば固定3ライン(`unassigned`/`team-a`/`team-b`)を投入する(`server/src/hocuspocus/seedExtension.ts`)。加えてクライアント側にライン追加UIがあり(`useBoardActions.addLine`)、末尾(既存ラインの最大`order`より後ろ)に新規ラインを`fractional index`方式で追加できる。追加したラインは末尾ページに表示されるようページを自動遷移する。ライン削除・並び替えUIは未実装
- **Yjsドキュメント名/WebSocketパス**: ドキュメント名は固定文字列`realtimeapp-space`(`YJS_DOCUMENT_NAME`、`packages/shared`で定義)。WebSocketは`/collaboration`パスのみ受け付け、ExpressのREST APIと同一HTTPサーバー・同一Node.jsプロセスで待ち受ける(`server/src/index.ts`)
- **認証連携**: HocuspocusProviderのコンストラクタに渡す`token`(JWT)を`onAuthenticate`フックで検証し、`documentName`が`YJS_DOCUMENT_NAME`と一致しない接続は拒否する
- **永続化のデバウンス**: Hocuspocus `Server.configure({ debounce: 2000, maxDebounce: 10000 })`により、確定操作から2秒後(最大10秒後)にスナップショットをPostgreSQLへ保存
- **Awareness状態の形**: `{ user: {id, name, color}, draggingPanelId: string | null, pendingMoves: PendingMove[] }`。`color`はuserIdから決定的に導出(サーバー同期不要)。ドラッグ開始時に`draggingPanelId`をセットし、ドロップ確定 or キャンセルで`null`に戻す。ドラッグ中のライブ位置(index)はAwarenessでは共有しない(Y.Docは確定時のみ更新のため、他ユーザー画面では「誰がどのパネルを編集中か」のバッジ表示のみ行う)。`pendingMoves`はドロップ後・確定前の「操作中」の移動を配列で保持し、ユーザー1人につき複数件同時に持てる(後述)
- **REST APIの認証**: `POST /history`・`GET /history`はともに`Authorization: Bearer <JWT>`必須
- **パスワードハッシュ**: Windows開発環境でのネイティブビルド依存を避けるため`bcrypt`ではなく`bcryptjs`を使用
- **サーバーのビルド**: `packages/shared`はビルドせずTSソースを`main`/`types`として直接参照するため、`server`は`tsc`でのdist生成をせず`tsx`で直接実行する(`npm run dev`/`npm run start`)。型チェックのみ`npm run typecheck --workspace=server`
- **PostgreSQLの構築**: ローカル開発ではリポジトリルートの`docker-compose.yml`で`postgres:16-alpine`を起動する(`docker compose up -d`)。認証情報・DB名・ポートは`server/.env.example`の`DATABASE_URL`と一致させる(`user`/`password`/`realtimeapp`/`5432`)。データは名前付きボリューム`postgres-data`に永続化

## 11. 追加要件(実装済み)

### 11.1 操作の確定フロー(操作中モード)

- パネルのドラッグを開始すると、そのパネルは従来通り「編集中」状態になり、他ユーザーにも表示される
- パネルをドロップした時点では即座に確定しない。ドロップされた側のライン(移動先)と、ドラッグ元だったライン(移動元)の両方が「操作中」モードになる(同一ライン内での並び替えの場合は移動元・移動先が同じラインになる)
- 「操作中」モードのライン(または操作)には確定ボタンが表示され、これを押すことで初めて操作が確定する
- 操作履歴(History)は確定ボタンが押されたタイミングでのみ更新される。ドロップ後・確定前の状態は履歴に記録されない
- 「操作中」モードは確定前であればキャンセル可能。キャンセルするとドラッグ開始前の状態に戻り、Y.Doc(永続構造)・履歴のどちらも変更されない
- **ロックの範囲はライン単位、かつ他ユーザーに対してのみ**: 「操作中」になるのは移動元・移動先の2ラインのみで、それ以外のラインは通常通り操作(追加・削除・D&D)できる。1回パネル操作を行っただけで画面全体が編集不可になることはない。さらに、このロックは**自分以外のユーザー**の操作をブロックするためのものであり、自分自身が「操作中」にしたライン(=自分の未確定操作のfrom/to)は自分に対してはロックされない。これにより、ドロップして確定バーが出た後でも、確定/キャンセルする前に自分でそのパネルを再度ドラッグして位置を調整したり、同じライン上で他のパネルの追加・削除を続けたりできる(操作中に操作不可にならないようにするため)
- 1人のユーザーが同時に複数の未確定操作を抱えられる(それぞれ別パネルであれば)。**同一パネル**について確定前に再度ドラッグ&ドロップした場合は、新しい未確定操作として積み上がるのではなく、既存の未確定操作を置き換える(常にパネルごとに最大1件)。動かした結果が直近の確定済み状態と一致する(元の位置に戻した)場合は、未確定操作自体が自動的に解消される
- **確定モーダルは常に1つ**: 未確定操作が何件あっても(=「操作中」ラインがいくつあっても)、画面下部に表示される確定バー/モーダルは常に1つだけで、「操作を確定しますか?」というメッセージも1回しか出ない。「確定」を押すとその時点の未確定操作をすべてまとめて確定し(履歴にはパネルごとに1件ずつ、合計で未確定操作の件数分記録される)、「キャンセル」を押すとすべての未確定操作をまとめて破棄する(個別の操作ごとの確定/キャンセルはできない)

### 11.2 ページング

- ライン一覧は10ラインごとにページングして表示する
- パネルに対する操作(追加・削除・D&D)は表示中のページ単位で行う(ページをまたいだパネル移動の扱いは未定・要検討)
- 影響範囲: 1節(概要)・3節(想定ユースケース)・7節(MVPスコープ)は本要件を反映済み。10節のライン仕様(固定3ライン)はページングを要するほどのライン数を想定していないため、ライン数が増える運用を見据える場合は再検討が必要
